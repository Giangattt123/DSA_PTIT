#include<bits/stdc++.h>
using ll = long long;
using namespace std;
#define endl "\n"
int main(){
	string a , b;
	cin >> a >> b;
	sort(a.begin(),a.end());
	sort(b.begin(),b.end());
	for(int i=0;i<a.size())
}
